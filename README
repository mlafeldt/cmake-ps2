This sample project shows you how the powerful build system CMake can be used
for the PS2.

First, copy cmake/EE.cmake to /usr/share/cmake-<VERSION>/Modules/Platform/.

Now you can build the samples:

$ ./build.sh <sample source directory>


STATUS

EE BIN  - Output is the same as with Makefile.
EE ERL  - Output is the same as with Makefile.
EE LIB  - Output seems to be the same as with Makefile.
IOP BIN - Not supported yet.
IOP LIB - Not supported yet.
